<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Base application theme -->
    <style name="Theme.FreelanceX" parent="android:Theme.Material.Light.NoActionBar">
        <!-- Status bar -->
        <item name="android:statusBarColor">@color/blue_600</item>
        <item name="android:windowLightStatusBar">false</item>
        <item name="android:colorBackground">@color/white</item>
    </style>
    
    <!-- Splash screen theme -->
    <style name="Theme.FreelanceX.Splash" parent="Theme.FreelanceX">
        <item name="android:windowBackground">@color/blue_600</item>
    </style>
</resources>